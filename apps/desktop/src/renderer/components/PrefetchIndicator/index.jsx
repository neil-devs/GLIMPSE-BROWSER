/**
 * @fileoverview PrefetchIndicator — now integrated into Toolbar.
 * This is a no-op wrapper kept for backward compatibility.
 */

import React from 'react';

export default function PrefetchIndicator() {
  /* Prefetch status is now shown directly in the Toolbar component */
  return null;
}
